---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: cfssl
      image: golang
      image_version: latest
      ansible_groups:
        - group1
verifier:
  name: testinfra
